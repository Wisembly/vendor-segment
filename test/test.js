(function () {
	describe('Segment test suite', function () {

		describe('General API', function () {
			it('should be defined', function () {
				expect(window.WisemblySegment).to.be.an('object');
			});
		});

	});
}());
